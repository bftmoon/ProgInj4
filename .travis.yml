language: java

addons:
  sonarcloud:
    organization: "bigfatmoon" # the key of the org you chose at step #3
#    token:
#      secure: $SONAR_TOKEN # encrypted value of your token
script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
#  - mvn clean verify sonar:sonar -Pcoverage -Dsonar.projectKey=bigfatmoon_ProgInj4
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.login=${SONAR_TOKEN}